// The JASS TECHNOLOGIES site is built as static HTML pages in the `public/`
// folder with vanilla JS hitting the FastAPI backend (see public/js/*.js).
// We keep the React entry point but render nothing so it doesn't overlay
// the real site.
function App() {
  return null;
}

export default App;
